{
  "name": "okta-demo-platform",
  "version": "1.0.0",
  "description": "Okta SE Demo Platform — Identity Flow Explorer",
  "scripts": {
    "dev": "concurrently -n API,UI -c cyan,magenta \"node server.js\" \"vite --port 5173\"",
    "build": "vite build",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.4",
    "concurrently": "^8.2.2",
    "vite": "^4.5.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
